npm install		// for node modules
npm install --save react-top-loading-bar	// For the top loading bar
npm install --save react-infinite-scroll-component		// For the infinity scroll

npm install react-router-dom	// For the routing of the app
npm start  // To launch the app 
